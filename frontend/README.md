# Pharmacy Assistant Tester

Фронтенд для тестирования API умного аптечного ассистента.

## Возможности

- 💬 **Чат-интерфейс** — полноценный диалог с ассистентом
- 🧪 **Тест-кейсы** — готовые сценарии для проверки разных интентов
- 🔍 **Debug-панель** — детальный просмотр запросов и ответов API
- ⚙️ **Настройки контекста** — user_id, ui_state для персонализации
- 📊 **Метрики** — отображение latency, confidence, actions

## Запуск

```bash
# Установка зависимостей
npm install

# Запуск dev сервера (порт 3000)
npm run dev
```

> ⚠️ Бэкенд должен быть запущен на `http://localhost:8000`

## Структура

```
frontend/
├── src/
│   ├── App.tsx         # Главный компонент
│   ├── api.ts          # API клиент
│   ├── types.ts        # TypeScript типы
│   ├── testCases.ts    # Библиотека тест-кейсов
│   ├── index.css       # Стили (Monokai Pro тема)
│   └── main.tsx        # Entry point
├── public/
│   └── vite.svg        # Фавикон
└── package.json
```

## Категории тест-кейсов

| Категория   | Описание                                    |
|-------------|---------------------------------------------|
| Поиск       | FIND_PRODUCT_BY_NAME, FIND_BY_SYMPTOM, etc. |
| Корзина     | SHOW_CART, ADD_TO_CART, REMOVE_FROM_CART    |
| Заказы      | ORDER_HISTORY, ORDER_STATUS, CANCEL_ORDER   |
| Профиль     | SHOW_PROFILE, FAVORITES, BONUS_BALANCE      |
| Аптеки      | NEARBY_PHARMACIES, PHARMACY_AVAILABILITY    |
| Информация  | RETURN_POLICY, DELIVERY_RULES               |
| Общение     | SMALL_TALK, UNKNOWN                         |
| Сложные     | Мульти-интенты, слот-филлинг                |

## Технологии

- React 18
- TypeScript
- Vite
- CSS Variables + Custom Theme

